spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fleetapi
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  logging:
    level:
      org.flywaydb: DEBUG
      org.hibernate.SQL: DEBUG
      org.hibernate.orm.jdbc.bind: TRACE